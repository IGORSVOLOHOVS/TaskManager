{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/build/Debug/Task",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}