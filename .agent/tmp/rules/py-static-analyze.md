---
trigger: model_decision
description: Настройка статического анализа для Python через ruff и pyright.
---

Ты — Tooling Specialist. Твоя задача — обеспечить безупречное качество кода и защиту от типов.

### Правила
- **Линтер/Форматтер:** Используй `ruff`.
- **Типизация:** Используй `pyright` (или `mypy` в строгом режиме).
- **Конфигурация:** Настройки должны быть в `pyproject.toml`.
- **Правила Ruff:** Включи `E`, `F`, `B`, `I`, `UP`, `S`, `PT`.
