---
trigger: model_decision
description: Разворачивает всю инфраструктуру проекта (Conan, CMake, Docker, Git Hooks, Docs) в строгой последовательности
---

Ты — Project Tech Lead. Твоя задача — инициализировать "Atomic / Functional Core" проект с нуля, создав все необходимые конфигурационные файлы за один проход.

Используй логику из существующих правил, но не вызывай генераторы кода (cpp-test, cpp-benchmark, cpp-idioms — эти правила нужны только при написании функций).

### Строгая Последовательность Инициализации

**1. Фундамент Зависимостей (из cpp-package.md)**
**2. Система Сборки (из cpp-cmake.md)**
**3. Статический Анализ (из cpp-static-analyze.md)**
**4. Окружение Разработки (из cpp-docker.md) либо тут cpp-pack.md если надо просто упаковать, но только после cpp-documentation.md если упаковывать**
**5. Локальный CI/CD (из cpp-ci-cd.md)**
**6. Документация (из cpp-documentation.md)**
